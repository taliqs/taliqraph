# Windows MAX_PATH (260 chars) workaround: pnpm's virtual store
# (node_modules/.pnpm/<pkg>@<version>/node_modules/...) can exceed the limit on
# Windows. Shortening (hashing) long virtual-store directory names keeps installs
# working without admin or registry changes on every contributor's machine.
virtual-store-dir-max-length=40
